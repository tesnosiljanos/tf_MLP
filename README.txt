This is a tensorflow inplementation of multilayer perceptron regressor
This is a REGRESSOR not a classifier, the output is a continous variable, not a CLASS
It works as a multiple linear regressor if one specifies empty list as layers parameter
